import Foundation

/// Constant values that are used within `scrub` and `scrub-service`.
public struct ScrubServiceConstants {

    /// Name of the `MachService`.
    public static let serviceName = "com.seinmon.scrub-service"
}
